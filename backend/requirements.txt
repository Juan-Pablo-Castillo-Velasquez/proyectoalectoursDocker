fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.31

sqlalchemy==2.0.41
psycopg2-binary==2.9.9
alembic==1.13.2

pydantic==2.9.2
pydantic-settings==2.5.2

python-dotenv==1.2.2

passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# 3.5.0 (no 3.4.0): 3.4.0 fuerza pyasn1<0.5.0, que quedó con CVEs conocidos
# sin parchear (PYSEC-2026-2263/3455/3456/3457) y sin forma de resolverlos
# manteniendo esa versión. 3.5.0 solo relaja esa cota (pyasn1>=0.5.0) sin
# cambiar la API que usa este proyecto (jwt.encode/jwt.decode/JWTError, ver
# app/core/security.py) — verificado con pytest tras el bump.
python-jose[cryptography]==3.5.0
pyasn1==0.6.4

fastapi-mail==1.4.1
email-validator==2.2.0

redis==5.0.8

# Observabilidad: monitoreo de errores y excepciones en tiempo real con
# Sentry. Se activa solo si SENTRY_DSN está definido en el entorno (ver
# app/core/config.py y app/main.py) — sin DSN la app arranca igual.
sentry-sdk[fastapi]==2.18.0

# Hosting externo OPCIONAL de imágenes (fotos de perfil, banners) en
# Cloudinary — ver app/core/image_storage.py. Sin CLOUDINARY_URL definida
# en el entorno, el proyecto sigue guardando en disco local exactamente
# como antes de este paquete: no es un requisito para levantar la app.
cloudinary==1.46.2
